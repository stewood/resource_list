# Staging Environment Configuration Example
# Copy this file and update the values for your staging environment
# These variables should be set in your Render service environment variables

# Django Settings
SECRET_KEY=django-insecure-staging-key-change-me-in-production
DEBUG=0
ALLOWED_HOSTS=isaiah58-resource-directory.onrender.com,localhost,127.0.0.1,0.0.0.0

# Database Configuration (PostgreSQL on Render)
DB_NAME=isaiah58_resources
DB_USER=isaiah58_user
DB_PASSWORD=CMXAq8v3zpy6Vwm1CIV26EKHagUDt0Nr
DB_HOST=dpg-d2lr5pre5dus7394h7f0-a.oregon-postgres.render.com
DB_PORT=5432

# Application Settings
GIS_ENABLED=0
DJANGO_SETTINGS_MODULE=resource_directory.staging_settings

# Static Files
STATIC_URL=/static/
STATIC_ROOT=/app/staticfiles

# Media Files
MEDIA_URL=/media/
MEDIA_ROOT=/app/media

# Security Settings
SECURE_BROWSER_XSS_FILTER=1
SECURE_CONTENT_TYPE_NOSNIFF=1
X_FRAME_OPTIONS=DENY

# Logging
LOG_LEVEL=INFO

# Cache (memory cache for staging)
CACHE_BACKEND=django.core.cache.backends.locmem.LocMemCache

# Email (console backend for staging)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
